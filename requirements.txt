# Thera-RAG: Two-App Architecture
#
# This is the root requirements file for development setup.
# For production, use app-specific requirements:
#
# - apps/ingestion/requirements.txt   (local processing)
# - apps/inference/requirements.txt   (streamlit deployment)

# Development and testing dependencies
python-dotenv==1.1.0
pytest>=7.0.0

# Install app-specific dependencies with:
# pip install -r apps/ingestion/requirements.txt    # For document processing
# pip install -r apps/inference/requirements.txt    # For chatbot development